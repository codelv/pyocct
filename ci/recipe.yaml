context: {}
package:

  name: pyocct
  version: 7.9.1

source:
  path: ../

build:
  number: 100

requirements:
  build:
    - '{{ compiler("cxx") }}'
    - sel(win): '{{ compiler("cxx") }}'
    - sel(win): glew
    - sel(linux): '{{ cdt("libxi-devel") }}'
    - sel(linux): '{{ cdt("libxext-devel") }}'
    - sel(linux): libegl-devel
    - sel(linux): libglx-devel
    - sel(linux): wayland
    - cmake
    - ninja
    - rapidjson
    - python
    - setuptools

  host:
    - python
    - pybind11
    - setuptools
    - occt ==7.9.1

  run:
    - python
    - occt ==7.9.1

test:
  source_files:
    - test

about:
  home: https://github.com/trelau/pyOCCT
  license: LGPL2.1
  license_family: LGPL
  summary: Python bindings for OpenCASCADE.
  description: Python bindings for OpenCASCADE geometry kernel.

